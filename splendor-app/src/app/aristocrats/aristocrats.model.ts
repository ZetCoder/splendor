export class Aristocrats {
    points: number;
    price: Object;
    imgUrl: string;

    constructor(points: number, price: Object, imgUrl: string) {
        this.points = points;
        this.price = price;
        this.imgUrl = imgUrl;
    }
}