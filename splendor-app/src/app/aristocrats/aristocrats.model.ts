export class Aristocrats {
    points: number;
    price: string[];
    imgUrl: string;

    constructor(points: number, price: string[], imgUrl: string) {
        this.points = points;
        this.price = price;
        this.imgUrl = imgUrl;
    }
}